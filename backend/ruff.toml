line-length = 100
# Match the Python 3.13 runtime used by CI and production.
target-version = "py313"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM", "PL", "RUF"]

[lint.per-file-ignores]
"tests/**/*.py" = ["PLR2004", "S101"]